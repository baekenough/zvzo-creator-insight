# 05. 페이지 UI 스펙

> **프로젝트**: zvzo-creator-insight
> **작성일**: 2026-02-04
> **버전**: 1.0.0

## 목차

1. [공통 레이아웃](#1-공통-레이아웃)
2. [디자인 시스템](#2-디자인-시스템)
3. [페이지 1: 랜딩 페이지](#3-페이지-1-랜딩-페이지)
4. [페이지 2: 대시보드](#4-페이지-2-대시보드)
5. [페이지 3: 크리에이터 상세](#5-페이지-3-크리에이터-상세)
6. [페이지 4: AI 제품 매칭](#6-페이지-4-ai-제품-매칭)
7. [컴포넌트 라이브러리](#7-컴포넌트-라이브러리)
8. [애니메이션 및 인터랙션](#8-애니메이션-및-인터랙션)

---

## 1. 공통 레이아웃

### 1.1 반응형 브레이크포인트

```typescript
// tailwind.config.ts 기본 브레이크포인트 사용
const breakpoints = {
  mobile: '0px',      // ~768px
  tablet: '768px',    // 768px ~ 1024px
  desktop: '1024px',  // 1024px ~
};
```

### 1.2 Header (헤더)

#### ASCII 와이어프레임

```
Desktop (1024px~):
┌──────────────────────────────────────────────────────────────────┐
│  [ZVZO Logo]  대시보드  |  소개                    [GitHub Icon]  │
└──────────────────────────────────────────────────────────────────┘

Mobile (~768px):
┌──────────────────────────────────────────────────────────────────┐
│  [☰]                    [ZVZO Logo]                  [GitHub]    │
└──────────────────────────────────────────────────────────────────┘
```

#### 구조

```typescript
// src/components/layout/header.tsx
interface HeaderProps {
  currentPath?: string;
}

interface NavItem {
  label: string;
  href: string;
  active?: boolean;
}
```

#### 상세 스펙

**Desktop (1024px~)**
- 높이: `64px` (h-16)
- 배경: `bg-white` + `border-b border-gray-200`
- 그림자: `shadow-sm`
- 패딩: `px-6 md:px-8 lg:px-12`
- 레이아웃: Flexbox, `justify-between items-center`

**로고**
- 위치: 좌측
- 텍스트: "ZVZO Creator Insight"
- 폰트: `text-xl font-bold`
- 색상:
  - "ZVZO": `text-[#236AF7]` (브랜드 컬러)
  - "Creator Insight": `text-gray-900`
- 호버: `hover:opacity-80 transition-opacity`

**네비게이션**
- 위치: 중앙
- 간격: `gap-8`
- 아이템:
  - "대시보드" → `/dashboard`
  - "소개" → `/about`
- 스타일:
  - 기본: `text-gray-600 text-sm font-medium`
  - 호버: `hover:text-[#236AF7] transition-colors`
  - Active: `text-[#236AF7] border-b-2 border-[#236AF7]`

**GitHub 링크**
- 위치: 우측
- 아이콘: lucide-react `<Github className="w-5 h-5" />`
- 색상: `text-gray-600`
- 호버: `hover:text-[#236AF7] transition-colors`

**Mobile (~768px)**
- 햄버거 메뉴: `<Menu className="w-6 h-6" />`
- 메뉴 버튼 위치: 좌측
- 로고: 중앙 배치
- GitHub 아이콘: 우측 유지

**모바일 메뉴 (슬라이드 패널)**
- 너비: `w-64` (256px)
- 배경: `bg-white`
- 그림자: `shadow-xl`
- 애니메이션: 좌측에서 슬라이드 인
- 오버레이: `bg-black/50` (배경 클릭 시 닫힘)
- 메뉴 아이템:
  - 패딩: `py-3 px-6`
  - 구분선: `border-b border-gray-200`

#### 상태

```typescript
type HeaderState =
  | 'default'        // 기본 상태
  | 'mobileOpen';    // 모바일 메뉴 열림

// 모바일 메뉴 상태 관리
const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
```

---

### 1.3 Footer (푸터)

#### ASCII 와이어프레임

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  ZVZO Creator Insight                                            │
│  AI 기반 크리에이터 판매 분석 플랫폼                              │
│                                                                  │
│  © 2026 ZVZO. All rights reserved.                               │
│  [GitHub] [Documentation] [Contact]                              │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**레이아웃**
- 높이: `auto` (최소 `min-h-32`)
- 배경: `bg-gray-50`
- 패딩: `py-8 px-6 md:px-8 lg:px-12`
- 상단 경계: `border-t border-gray-200`

**콘텐츠**
- 정렬: 중앙 (`text-center`)
- 제목: `text-lg font-semibold text-gray-900 mb-2`
- 설명: `text-sm text-gray-600 mb-4`
- 저작권: `text-xs text-gray-500 mb-4`

**링크**
- 간격: `gap-4`
- 스타일: `text-sm text-gray-600 hover:text-[#236AF7]`
- 구분자: `|` (파이프)

---

### 1.4 공통 페이지 컨테이너

#### 스펙

```typescript
// src/components/layout/page-container.tsx
interface PageContainerProps {
  children: React.ReactNode;
  maxWidth?: 'sm' | 'md' | 'lg' | 'xl' | 'full';
  className?: string;
}

const maxWidthClasses = {
  sm: 'max-w-screen-sm',   // 640px
  md: 'max-w-screen-md',   // 768px
  lg: 'max-w-screen-lg',   // 1024px
  xl: 'max-w-screen-xl',   // 1280px
  full: 'max-w-full',
};
```

**기본 스타일**
- 최대 너비: `max-w-screen-xl` (1280px)
- 중앙 정렬: `mx-auto`
- 패딩:
  - Mobile: `px-4 py-6`
  - Tablet: `md:px-6 md:py-8`
  - Desktop: `lg:px-12 lg:py-10`

---

## 2. 디자인 시스템

### 2.1 색상 팔레트

#### Primary (브랜드 컬러)

```typescript
const primary = {
  DEFAULT: '#236AF7',    // 메인 브랜드 컬러
  hover: '#1B5AD4',      // 호버 상태 (-15% 밝기)
  light: '#E8F0FE',      // 배경용 밝은 색
  dark: '#1A4FB8',       // 강조용 어두운 색
};
```

**사용 예**
- 버튼 배경: `bg-[#236AF7] hover:bg-[#1B5AD4]`
- 텍스트 강조: `text-[#236AF7]`
- 배경 영역: `bg-[#E8F0FE]`

#### Secondary (보조 색상)

```typescript
const secondary = {
  DEFAULT: '#6B7280',    // 그레이
  light: '#9CA3AF',
  dark: '#4B5563',
};
```

#### Semantic (의미론적 색상)

```typescript
const semantic = {
  success: {
    DEFAULT: '#10B981',  // 녹색
    light: '#D1FAE5',
    dark: '#059669',
  },
  warning: {
    DEFAULT: '#F59E0B',  // 주황
    light: '#FEF3C7',
    dark: '#D97706',
  },
  error: {
    DEFAULT: '#EF4444',  // 빨강
    light: '#FEE2E2',
    dark: '#DC2626',
  },
  info: {
    DEFAULT: '#3B82F6',  // 파랑
    light: '#DBEAFE',
    dark: '#2563EB',
  },
};
```

#### Background & Surface

```typescript
const background = {
  DEFAULT: '#FFFFFF',    // 기본 배경
  surface: '#F9FAFB',    // 카드/섹션 배경
  hover: '#F3F4F6',      // 호버 배경
};

const border = {
  DEFAULT: '#E5E7EB',    // 기본 테두리
  light: '#F3F4F6',
  dark: '#D1D5DB',
};
```

#### Text

```typescript
const text = {
  primary: '#111827',    // 제목/본문
  secondary: '#6B7280',  // 부제/설명
  muted: '#9CA3AF',      // 비활성/힌트
  inverse: '#FFFFFF',    // 다크 배경 위 텍스트
};
```

---

### 2.2 타이포그래피

#### 폰트 패밀리

```typescript
// Pretendard 폰트 사용 (가독성 최적화된 한글 폰트)
const fontFamily = {
  sans: ['Pretendard', 'system-ui', 'sans-serif'],
  mono: ['JetBrains Mono', 'monospace'],
};
```

**폰트 로딩**
```typescript
// src/app/layout.tsx
import localFont from 'next/font/local';

const pretendard = localFont({
  src: '../fonts/PretendardVariable.woff2',
  variable: '--font-pretendard',
  display: 'swap',
});
```

#### 폰트 크기 스케일

```typescript
const fontSize = {
  xs: '0.75rem',     // 12px - 캡션, 뱃지
  sm: '0.875rem',    // 14px - 부가 정보
  base: '1rem',      // 16px - 본문
  lg: '1.125rem',    // 18px - 소제목
  xl: '1.25rem',     // 20px - 카드 제목
  '2xl': '1.5rem',   // 24px - 섹션 제목
  '3xl': '1.875rem', // 30px - 페이지 제목
  '4xl': '2.25rem',  // 36px - 히어로 제목
};
```

#### 폰트 굵기

```typescript
const fontWeight = {
  regular: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
};
```

#### 줄 간격

```typescript
const lineHeight = {
  tight: 1.25,    // 제목용
  normal: 1.5,    // 본문용
  relaxed: 1.75,  // 긴 텍스트용
};
```

#### 텍스트 스타일 프리셋

```typescript
const textStyles = {
  h1: 'text-4xl font-bold leading-tight text-gray-900',
  h2: 'text-3xl font-bold leading-tight text-gray-900',
  h3: 'text-2xl font-semibold leading-tight text-gray-900',
  h4: 'text-xl font-semibold leading-tight text-gray-900',
  body: 'text-base font-regular leading-normal text-gray-700',
  small: 'text-sm font-regular leading-normal text-gray-600',
  caption: 'text-xs font-regular leading-normal text-gray-500',
};
```

---

### 2.3 간격 (Spacing)

#### 기본 스케일 (4px 기준)

```typescript
const spacing = {
  0: '0px',
  1: '4px',     // 0.25rem
  2: '8px',     // 0.5rem
  3: '12px',    // 0.75rem
  4: '16px',    // 1rem
  5: '20px',    // 1.25rem
  6: '24px',    // 1.5rem
  8: '32px',    // 2rem
  10: '40px',   // 2.5rem
  12: '48px',   // 3rem
  16: '64px',   // 4rem
  20: '80px',   // 5rem
};
```

#### 사용 가이드

| 용도 | 권장 간격 | Tailwind 클래스 |
|------|----------|----------------|
| 컴포넌트 내 요소 간격 | 4-8px | `gap-1 ~ gap-2` |
| 카드 내부 패딩 | 16-24px | `p-4 ~ p-6` |
| 섹션 간 여백 | 32-48px | `space-y-8 ~ space-y-12` |
| 페이지 상단 여백 | 40-64px | `pt-10 ~ pt-16` |

---

### 2.4 테두리 (Border)

#### Border Radius

```typescript
const borderRadius = {
  none: '0',
  sm: '4px',     // 작은 요소 (버튼, 뱃지)
  md: '8px',     // 기본 (카드, 입력 필드)
  lg: '12px',    // 큰 카드
  xl: '16px',    // 섹션
  full: '9999px', // 원형 (아바타, 아이콘)
};
```

#### Border Width

```typescript
const borderWidth = {
  DEFAULT: '1px',
  0: '0',
  2: '2px',
  4: '4px',
};
```

---

### 2.5 그림자 (Shadow)

```typescript
const boxShadow = {
  sm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
  // 작은 카드, 호버 상태

  md: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
  // 기본 카드

  lg: '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
  // 모달, 드롭다운

  xl: '0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04)',
  // 강조용 큰 카드
};
```

**사용 예**
- 기본 카드: `shadow-md`
- 호버 시: `hover:shadow-lg transition-shadow`
- 모달: `shadow-xl`

---

## 3. 페이지 1: 랜딩 페이지

### 3.1 전체 와이어프레임 (Desktop)

```
┌────────────────────────────────────────────────────────────────┐
│ [ZVZO Logo]  대시보드 | 소개                      [GitHub]      │ ← Header
├────────────────────────────────────────────────────────────────┤
│                                                                │
│                     [히어로 섹션]                               │
│                                                                │
│              데이터로 찾는 최적의                               │
│            크리에이터-제품 매칭                                 │
│                                                                │
│      AI 분석으로 매출을 예측하고 최고의 파트너를 찾으세요        │
│                                                                │
│                  [데모 시작하기 버튼]                           │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│                    [핵심 가치 3개]                              │
│                                                                │
│  ┌────────────┐   ┌────────────┐   ┌────────────┐            │
│  │ [Icon]     │   │ [Icon]     │   │ [Icon]     │            │
│  │            │   │            │   │            │            │
│  │ AI 판매    │   │ 스마트     │   │ 매출       │            │
│  │ 분석       │   │ 매칭       │   │ 예측       │            │
│  │            │   │            │   │            │            │
│  └────────────┘   └────────────┘   └────────────┘            │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│                   [작동 방식 3단계]                             │
│                                                                │
│    1. 데이터 수집  →  2. AI 분석  →  3. 추천 & 예측          │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│                  [하단 CTA 반복]                                │
│                                                                │
│              지금 바로 시작해보세요                             │
│                [데모 시작하기 버튼]                             │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│                         [Footer]                               │
└────────────────────────────────────────────────────────────────┘
```

### 3.2 히어로 섹션

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                    [배경 그라데이션]                            │
│                                                                │
│              데이터로 찾는 최적의                               │
│            크리에이터-제품 매칭                                 │
│            ═══════════════════════                             │
│                                                                │
│      AI 분석으로 매출을 예측하고 최고의 파트너를 찾으세요        │
│                                                                │
│                  ┌──────────────┐                              │
│                  │ 데모 시작하기  │                             │
│                  └──────────────┘                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**레이아웃**
- 높이: `min-h-[600px] md:min-h-[700px]`
- 패딩: `py-20 md:py-32 px-6`
- 정렬: 중앙 (`flex flex-col items-center justify-center`)

**배경**
- 그라데이션:
  ```css
  bg-gradient-to-br from-[#E8F0FE] via-white to-[#F0F4FF]
  ```
- 선택적 배경 패턴: 미묘한 도트 패턴 (opacity: 0.3)

**제목 (h1)**
- 텍스트: "데이터로 찾는 최적의<br/>크리에이터-제품 매칭"
- 폰트: `text-4xl md:text-5xl lg:text-6xl font-bold`
- 색상: `text-gray-900`
- 줄 간격: `leading-tight`
- 애니메이션: fade-in + slide-up (0.6s ease-out)

**부제목**
- 텍스트: "AI 분석으로 매출을 예측하고 최고의 파트너를 찾으세요"
- 폰트: `text-lg md:text-xl`
- 색상: `text-gray-600`
- 여백: `mt-6 mb-10`
- 애니메이션: fade-in + slide-up (0.8s ease-out, delay 0.2s)

**CTA 버튼**
- 텍스트: "데모 시작하기"
- 스타일:
  ```typescript
  className="
    bg-[#236AF7] hover:bg-[#1B5AD4]
    text-white font-semibold
    px-8 py-4 rounded-lg
    shadow-lg hover:shadow-xl
    transition-all duration-300
    transform hover:scale-105
  "
  ```
- 아이콘: `<ArrowRight />` (우측)
- 링크: `/dashboard`
- 애니메이션: fade-in (1.0s ease-out, delay 0.4s)

**반응형**
- Mobile: 제목 2줄, 버튼 full-width
- Tablet: 제목 유지, 버튼 auto-width
- Desktop: 모든 요소 최대 크기

---

### 3.3 핵심 가치 섹션

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                        핵심 가치                                │
│                                                                │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐      │
│  │   [Icon]     │   │   [Icon]     │   │   [Icon]     │      │
│  │  BarChart3   │   │  Zap         │   │  TrendingUp  │      │
│  │              │   │              │   │              │      │
│  │ AI 판매 분석  │   │ 스마트 매칭   │   │ 매출 예측     │      │
│  │              │   │              │   │              │      │
│  │ 크리에이터의  │   │ 과거 성과를   │   │ AI가 예측하는 │      │
│  │ 판매 데이터를 │   │ 기반으로 최적 │   │ 향후 매출과   │      │
│  │ AI가 분석하여 │   │ 의 제품을     │   │ 신뢰 구간을   │      │
│  │ 인사이트 제공 │   │ 추천합니다    │   │ 제공합니다    │      │
│  │              │   │              │   │              │      │
│  └──────────────┘   └──────────────┘   └──────────────┘      │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**레이아웃**
- 패딩: `py-20 px-6`
- 배경: `bg-white`
- 최대 너비: `max-w-screen-xl mx-auto`

**섹션 제목**
- 텍스트: "핵심 가치"
- 폰트: `text-3xl md:text-4xl font-bold`
- 색상: `text-gray-900`
- 정렬: 중앙 (`text-center`)
- 여백: `mb-16`

**카드 그리드**
- 레이아웃: `grid grid-cols-1 md:grid-cols-3 gap-8`
- 각 카드:
  - 배경: `bg-gray-50`
  - 패딩: `p-8`
  - 테두리: `rounded-xl`
  - 그림자: `shadow-md hover:shadow-lg`
  - 전환: `transition-shadow duration-300`

**카드 구조**

1. **아이콘 영역**
   - 크기: `w-16 h-16`
   - 배경: `bg-[#236AF7]`
   - 모양: `rounded-lg`
   - 아이콘 색상: `text-white`
   - 여백: `mb-6`
   - 애니메이션: 호버 시 회전 (rotate-6)

2. **제목**
   - 폰트: `text-xl font-semibold`
   - 색상: `text-gray-900`
   - 여백: `mb-4`

3. **설명**
   - 폰트: `text-base`
   - 색상: `text-gray-600`
   - 줄 간격: `leading-relaxed`

**카드 내용**

| 카드 | 아이콘 | 제목 | 설명 |
|------|--------|------|------|
| 1 | `<BarChart3 />` | AI 판매 분석 | 크리에이터의 판매 데이터를 AI가 분석하여 카테고리별 강점, 가격대 성과, 시즌별 트렌드 인사이트를 제공합니다 |
| 2 | `<Zap />` | 스마트 매칭 | 과거 성과를 기반으로 최적의 제품을 추천하며, 매칭 점수와 근거를 명확히 제시합니다 |
| 3 | `<TrendingUp />` | 매출 예측 | AI가 예측하는 향후 매출과 신뢰 구간을 제공하여 데이터 기반 의사결정을 지원합니다 |

**애니메이션**
- 스크롤 시 카드별로 순차적 fade-in
- 딜레이: 0.1s씩 증가 (stagger effect)
- 호버 시: scale-105 + shadow-lg

---

### 3.4 작동 방식 섹션

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                      작동 방식                                  │
│                                                                │
│  ┌────────┐         ┌────────┐         ┌────────┐            │
│  │   1    │   ───>  │   2    │   ───>  │   3    │            │
│  │ 데이터  │         │   AI   │         │  추천   │            │
│  │  수집   │         │  분석  │         │ & 예측  │            │
│  └────────┘         └────────┘         └────────┘            │
│      │                   │                   │                │
│      ▼                   ▼                   ▼                │
│  크리에이터의          판매 패턴과           최적 제품과        │
│  과거 판매 데이터       카테고리 강점을       예상 매출을        │
│  자동 수집             AI가 분석             추천               │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**레이아웃**
- 패딩: `py-20 px-6`
- 배경: `bg-gray-50`
- 최대 너비: `max-w-screen-xl mx-auto`

**섹션 제목**
- 텍스트: "작동 방식"
- 폰트: `text-3xl md:text-4xl font-bold`
- 색상: `text-gray-900`
- 정렬: 중앙
- 여백: `mb-16`

**스텝 레이아웃**
- 레이아웃: `flex flex-col md:flex-row items-center justify-between gap-8`
- 각 스텝 간 화살표: `<ArrowRight />` (desktop) 또는 `<ArrowDown />` (mobile)

**스텝 카드**
- 너비: `w-full md:w-64`
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border-2 border-gray-200`
- 그림자: `shadow-md`

**스텝 구조**

1. **번호 뱃지**
   - 크기: `w-12 h-12`
   - 배경: `bg-[#236AF7]`
   - 모양: `rounded-full`
   - 폰트: `text-xl font-bold text-white`
   - 위치: 상단 중앙
   - 여백: `mb-4`

2. **제목**
   - 폰트: `text-lg font-semibold`
   - 색상: `text-gray-900`
   - 정렬: 중앙
   - 여백: `mb-3`

3. **설명**
   - 폰트: `text-sm`
   - 색상: `text-gray-600`
   - 정렬: 중앙
   - 줄 간격: `leading-relaxed`

**스텝 내용**

| Step | 제목 | 설명 |
|------|------|------|
| 1 | 데이터 수집 | 크리에이터의 과거 판매 데이터를 자동으로 수집하고 정제합니다 |
| 2 | AI 분석 | 판매 패턴과 카테고리 강점을 AI가 심층 분석합니다 |
| 3 | 추천 & 예측 | 최적 제품과 예상 매출을 데이터 기반으로 추천합니다 |

**화살표**
- Desktop: 우측 화살표 (회색, 크기 24px)
- Mobile: 하단 화살표 (회색, 크기 24px)
- 애니메이션: 좌우(또는 상하) 이동 (pulse)

---

### 3.5 하단 CTA 섹션

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                  지금 바로 시작해보세요                          │
│                                                                │
│          ZVZO Creator Insight로 최적의 파트너를 찾으세요         │
│                                                                │
│                  ┌──────────────┐                              │
│                  │ 데모 시작하기  │                             │
│                  └──────────────┘                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**레이아웃**
- 패딩: `py-20 px-6`
- 배경: 그라데이션
  ```css
  bg-gradient-to-r from-[#236AF7] to-[#1B5AD4]
  ```
- 정렬: 중앙

**제목**
- 텍스트: "지금 바로 시작해보세요"
- 폰트: `text-3xl md:text-4xl font-bold`
- 색상: `text-white`
- 여백: `mb-4`

**부제목**
- 텍스트: "ZVZO Creator Insight로 최적의 파트너를 찾으세요"
- 폰트: `text-lg md:text-xl`
- 색상: `text-white/90`
- 여백: `mb-8`

**CTA 버튼**
- 텍스트: "데모 시작하기"
- 스타일:
  ```typescript
  className="
    bg-white hover:bg-gray-100
    text-[#236AF7] font-semibold
    px-8 py-4 rounded-lg
    shadow-xl
    transition-all duration-300
    transform hover:scale-105
  "
  ```
- 링크: `/dashboard`

---

### 3.6 반응형 동작

**Mobile (~768px)**
- 히어로 제목: 2줄, `text-4xl`
- 핵심 가치 카드: 1열 (세로 스택)
- 작동 방식: 세로 레이아웃 + 하단 화살표
- CTA 버튼: full-width

**Tablet (768px~1024px)**
- 히어로 제목: 1줄 또는 2줄, `text-5xl`
- 핵심 가치 카드: 3열 (작은 갭)
- 작동 방식: 가로 레이아웃 + 우측 화살표

**Desktop (1024px~)**
- 모든 요소 최대 크기
- 넉넉한 여백과 간격

---

## 4. 페이지 2: 대시보드

### 4.1 전체 와이어프레임 (Desktop)

```
┌────────────────────────────────────────────────────────────────┐
│ [ZVZO Logo]  대시보드 | 소개                      [GitHub]      │ ← Header
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  크리에이터 대시보드                                            │
│                                                                │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐          │
│  │ 총 크리  │  │ 총 매출  │  │ 평균    │  │ 상위    │          │
│  │ 에이터   │  │         │  │ 전환율   │  │ 카테고리 │          │
│  │ 20명    │  │ 580만원  │  │ 3.8%    │  │ Beauty  │          │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘          │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  [🔍 검색]  [플랫폼 ▼]  [카테고리 ▼]  [정렬 ▼]                 │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│  프로필  이름      플랫폼  팔로워   카테고리    매출    전환율   │
│  ─────────────────────────────────────────────────────────    │
│  [img]  김지은   [IG]   25만    Beauty      375만    4.2%    │
│  [img]  박준호   [YT]   58만    Tech        420만    2.5%    │
│  [img]  이서연   [IG]   38만    Fashion     320만    3.1%    │
│  ...                                                           │
│                                                                │
│  ◄ 1 2 3 4 5 ►                                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

### 4.2 상단 요약 카드

#### ASCII 와이어프레임

```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│ [Icon]      │  │ [Icon]      │  │ [Icon]      │  │ [Icon]      │
│ Users       │  │ DollarSign  │  │ Percent     │  │ Tag         │
│             │  │             │  │             │  │             │
│ 총 크리에이터 │  │ 총 매출      │  │ 평균 전환율  │  │ 상위 카테고리│
│ 20명        │  │ 5,800만원   │  │ 3.8%        │  │ Beauty      │
│             │  │             │  │             │  │             │
│ +2 이번 달  │  │ +15% 전월비 │  │ +0.3% 전월비│  │ 45건 판매   │
└─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘
```

#### 상세 스펙

**레이아웃**
- 그리드: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6`
- 여백: `mb-8`

**카드 공통 스타일**
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-sm hover:shadow-md`
- 전환: `transition-shadow duration-300`

**카드 구조**

1. **아이콘 + 레이블 행**
   - 레이아웃: `flex items-center justify-between mb-3`
   - 아이콘:
     - 크기: `w-10 h-10`
     - 배경: `bg-[#E8F0FE]`
     - 모양: `rounded-lg`
     - 아이콘 색상: `text-[#236AF7]`
   - 레이블:
     - 폰트: `text-sm font-medium`
     - 색상: `text-gray-600`

2. **값 (Main Metric)**
   - 폰트: `text-2xl md:text-3xl font-bold`
   - 색상: `text-gray-900`
   - 여백: `mb-2`

3. **변화량 (Subtext)**
   - 폰트: `text-xs md:text-sm`
   - 색상:
     - 증가: `text-green-600`
     - 감소: `text-red-600`
     - 중립: `text-gray-500`
   - 아이콘: `<TrendingUp />` 또는 `<TrendingDown />`

**카드 데이터**

| 카드 | 아이콘 | 레이블 | 값 | 변화량 |
|------|--------|--------|-----|--------|
| 1 | `<Users />` | 총 크리에이터 | 20명 | +2 이번 달 |
| 2 | `<DollarSign />` | 총 매출 | 58,000,000원 | +15% 전월 대비 |
| 3 | `<Percent />` | 평균 전환율 | 3.8% | +0.3%p 전월 대비 |
| 4 | `<Tag />` | 상위 카테고리 | Beauty | 45건 판매 |

**숫자 포맷팅**
```typescript
// 매출: 만원 단위 (5,800만원)
const formatRevenue = (value: number) => {
  return `${(value / 10000).toLocaleString()}만원`;
};

// 전환율: 소수점 1자리
const formatConversionRate = (value: number) => {
  return `${value.toFixed(1)}%`;
};
```

---

### 4.3 검색 및 필터 바

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  [🔍 크리에이터 검색...]  [플랫폼 ▼]  [카테고리 ▼]  [매출순 ▼]  │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**레이아웃**
- 레이아웃: `flex flex-col md:flex-row gap-4 mb-6`
- 배경: `bg-gray-50`
- 패딩: `p-4`
- 테두리: `rounded-lg border border-gray-200`

**검색 입력 필드**
- 너비: `flex-1 md:max-w-md`
- 플레이스홀더: "크리에이터 이름 검색..."
- 아이콘: `<Search className="w-5 h-5 text-gray-400" />` (좌측)
- 스타일:
  ```typescript
  className="
    w-full px-4 py-2.5 pl-10
    bg-white border border-gray-300 rounded-lg
    text-sm placeholder:text-gray-400
    focus:outline-none focus:ring-2 focus:ring-[#236AF7] focus:border-transparent
    transition-all duration-200
  "
  ```
- Debounce: 300ms (검색 API 호출 최적화)

**플랫폼 필터 (Dropdown)**
- 레이블: "플랫폼"
- 옵션:
  - 전체
  - Instagram
  - YouTube
  - TikTok
  - Blog
- 기본값: "전체"
- 스타일:
  ```typescript
  className="
    px-4 py-2.5 min-w-[140px]
    bg-white border border-gray-300 rounded-lg
    text-sm text-gray-700
    focus:outline-none focus:ring-2 focus:ring-[#236AF7]
    cursor-pointer
  "
  ```
- 아이콘: `<ChevronDown className="w-4 h-4" />` (우측)

**카테고리 필터 (Dropdown)**
- 레이블: "카테고리"
- 옵션:
  - 전체
  - Beauty
  - Fashion
  - Lifestyle
  - Food
  - Tech
  - HomeLiving
  - Health
  - BabyKids
  - Pet
  - Stationery
- 기본값: "전체"
- 다중 선택 가능 (Checkbox 포함)

**정렬 드롭다운**
- 레이블: "정렬"
- 옵션:
  - 매출 높은순
  - 매출 낮은순
  - 전환율 높은순
  - 전환율 낮은순
  - 팔로워 많은순
  - 팔로워 적은순
- 기본값: "매출 높은순"
- 아이콘: `<ArrowUpDown />` (정렬 방향 표시)

**필터 적용 로직**
```typescript
interface FilterState {
  searchQuery: string;
  platform: Platform | 'all';
  categories: Category[];
  sortBy: 'revenue' | 'conversionRate' | 'followerCount';
  sortOrder: 'asc' | 'desc';
}

// Debounced 검색
const [searchQuery, setSearchQuery] = useState('');
const debouncedSearch = useDebounce(searchQuery, 300);

// 필터링된 결과
const filteredCreators = useMemo(() => {
  let result = creators;

  // 검색어 필터
  if (debouncedSearch) {
    result = result.filter(c =>
      c.name.toLowerCase().includes(debouncedSearch.toLowerCase())
    );
  }

  // 플랫폼 필터
  if (filterState.platform !== 'all') {
    result = result.filter(c => c.platform === filterState.platform);
  }

  // 카테고리 필터
  if (filterState.categories.length > 0) {
    result = result.filter(c =>
      c.categories.some(cat => filterState.categories.includes(cat))
    );
  }

  // 정렬
  result.sort((a, b) => {
    const order = filterState.sortOrder === 'asc' ? 1 : -1;
    return (a[filterState.sortBy] - b[filterState.sortBy]) * order;
  });

  return result;
}, [creators, debouncedSearch, filterState]);
```

**반응형**
- Mobile: 세로 스택, 각 필터 full-width
- Tablet: 2x2 그리드
- Desktop: 가로 한 줄

---

### 4.4 크리에이터 테이블

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  프로필   이름      플랫폼   팔로워     카테고리   매출    전환율 │
│  ────────────────────────────────────────────────────────────  │
│  [img]   김지은    [IG]    250,000   Beauty    375만   4.2%   │
│  [img]   박준호    [YT]    580,000   Tech      420만   2.5%   │
│  [img]   이서연    [IG]    380,000   Fashion   320만   3.1%   │
│  [img]   최민수    [TK]    120,000   Food      180만   5.8%   │
│  [img]   정하은    [YT]    450,000   Beauty    310만   3.5%   │
│  ...                                                            │
│                                                                │
│  총 20개 중 1-10 표시                         ◄ 1 2 3 4 5 ►   │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**테이블 컨테이너**
- 배경: `bg-white`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-sm`
- 오버플로: `overflow-x-auto` (모바일 가로 스크롤)

**테이블 헤더**
- 배경: `bg-gray-50`
- 패딩: `px-6 py-4`
- 테두리: `border-b border-gray-200`
- 폰트: `text-xs font-semibold text-gray-600 uppercase tracking-wider`
- 정렬 가능 컬럼: 클릭 시 정렬 방향 변경
  - 정렬 아이콘: `<ArrowUp />` 또는 `<ArrowDown />`

**테이블 행**
- 패딩: `px-6 py-4`
- 테두리: `border-b border-gray-200 last:border-0`
- 호버: `hover:bg-gray-50 transition-colors`
- 클릭 가능: `cursor-pointer` → 크리에이터 상세 페이지 이동

**컬럼 상세**

1. **프로필 이미지**
   - 크기: `w-10 h-10 md:w-12 md:h-12`
   - 모양: `rounded-full`
   - 테두리: `ring-2 ring-gray-200`
   - 플레이스홀더: 이니셜 또는 기본 아바타

2. **이름**
   - 폰트: `text-sm md:text-base font-medium`
   - 색상: `text-gray-900`
   - 호버: `text-[#236AF7]`

3. **플랫폼 아이콘**
   - 크기: `w-6 h-6`
   - 배지 형태:
     ```typescript
     const platformBadges = {
       Instagram: {
         icon: '📷',
         bg: 'bg-pink-100',
         text: 'text-pink-700',
         label: 'IG',
       },
       YouTube: {
         icon: '▶',
         bg: 'bg-red-100',
         text: 'text-red-700',
         label: 'YT',
       },
       TikTok: {
         icon: '🎵',
         bg: 'bg-purple-100',
         text: 'text-purple-700',
         label: 'TK',
       },
       Blog: {
         icon: '📝',
         bg: 'bg-blue-100',
         text: 'text-blue-700',
         label: 'BL',
       },
     };
     ```
   - 스타일: `px-2 py-1 rounded text-xs font-medium`

4. **팔로워 수**
   - 폰트: `text-sm`
   - 색상: `text-gray-700`
   - 포맷: 천 단위 콤마 (250,000)
   - Mobile: 축약 표시 (250K)

5. **주력 카테고리 (뱃지)**
   - 최대 2개 표시 ("+1" 나머지 개수)
   - 스타일:
     ```typescript
     className="
       inline-block px-2.5 py-1 mr-2
       bg-[#E8F0FE] text-[#236AF7]
       text-xs font-medium rounded-full
     "
     ```
   - 카테고리별 색상:
     ```typescript
     const categoryColors = {
       Beauty: { bg: 'bg-pink-100', text: 'text-pink-700' },
       Fashion: { bg: 'bg-purple-100', text: 'text-purple-700' },
       Tech: { bg: 'bg-blue-100', text: 'text-blue-700' },
       Food: { bg: 'bg-green-100', text: 'text-green-700' },
       // ...
     };
     ```

6. **총 매출**
   - 폰트: `text-sm md:text-base font-semibold`
   - 색상: `text-gray-900`
   - 포맷: 만원 단위 (375만원)

7. **전환율**
   - 폰트: `text-sm`
   - 색상:
     - 4% 이상: `text-green-600 font-semibold`
     - 3-4%: `text-gray-900`
     - 3% 미만: `text-gray-600`
   - 포맷: 소수점 1자리 (4.2%)
   - 아이콘: 높은 전환율은 `<TrendingUp className="inline w-4 h-4" />`

**행 클릭 동작**
```typescript
const handleRowClick = (creatorId: string) => {
  router.push(`/creator/${creatorId}`);
};
```

**빈 상태 (검색 결과 없음)**

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                       [Search Icon]                            │
│                                                                │
│                 검색 결과가 없습니다                            │
│                                                                │
│             다른 검색어나 필터를 시도해보세요                   │
│                                                                │
│                  [필터 초기화 버튼]                             │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

**빈 상태 스펙**
- 높이: `min-h-[400px]`
- 정렬: 중앙 (`flex flex-col items-center justify-center`)
- 아이콘: `<SearchX className="w-16 h-16 text-gray-300 mb-4" />`
- 메시지: `text-lg font-medium text-gray-700 mb-2`
- 서브텍스트: `text-sm text-gray-500 mb-6`
- 버튼:
  ```typescript
  className="
    px-6 py-2.5 bg-gray-100 hover:bg-gray-200
    text-gray-700 font-medium rounded-lg
    transition-colors
  "
  ```

**로딩 상태 (스켈레톤 UI)**

```
┌────────────────────────────────────────────────────────────────┐
│  [▓]  ▓▓▓▓▓▓   [▓]  ▓▓▓▓▓▓  ▓▓▓▓▓  ▓▓▓▓▓▓  ▓▓▓               │
│  [▓]  ▓▓▓▓▓▓   [▓]  ▓▓▓▓▓▓  ▓▓▓▓▓  ▓▓▓▓▓▓  ▓▓▓               │
│  [▓]  ▓▓▓▓▓▓   [▓]  ▓▓▓▓▓▓  ▓▓▓▓▓  ▓▓▓▓▓▓  ▓▓▓               │
│  [▓]  ▓▓▓▓▓▓   [▓]  ▓▓▓▓▓▓  ▓▓▓▓▓  ▓▓▓▓▓▓  ▓▓▓               │
│  [▓]  ▓▓▓▓▓▓   [▓]  ▓▓▓▓▓▓  ▓▓▓▓▓  ▓▓▓▓▓▓  ▓▓▓               │
└────────────────────────────────────────────────────────────────┘
```

**스켈레톤 스펙**
- 행 개수: 5개 (고정)
- 애니메이션: `animate-pulse`
- 배경: `bg-gray-200`
- 모양: `rounded` (각 셀에 맞는 크기)

```typescript
// 스켈레톤 행 컴포넌트
const SkeletonRow = () => (
  <tr className="animate-pulse">
    <td className="px-6 py-4">
      <div className="w-12 h-12 bg-gray-200 rounded-full"></div>
    </td>
    <td className="px-6 py-4">
      <div className="h-4 bg-gray-200 rounded w-24"></div>
    </td>
    <td className="px-6 py-4">
      <div className="h-6 bg-gray-200 rounded-full w-12"></div>
    </td>
    <td className="px-6 py-4">
      <div className="h-4 bg-gray-200 rounded w-20"></div>
    </td>
    <td className="px-6 py-4">
      <div className="flex gap-2">
        <div className="h-6 bg-gray-200 rounded-full w-16"></div>
        <div className="h-6 bg-gray-200 rounded-full w-16"></div>
      </div>
    </td>
    <td className="px-6 py-4">
      <div className="h-4 bg-gray-200 rounded w-16"></div>
    </td>
    <td className="px-6 py-4">
      <div className="h-4 bg-gray-200 rounded w-10"></div>
    </td>
  </tr>
);
```

---

### 4.5 페이지네이션

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│  총 20개 중 1-10 표시              ◄ [1] 2  3  4  5 ... 10 ►  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**레이아웃**
- 위치: 테이블 하단
- 패딩: `px-6 py-4`
- 정렬: `flex items-center justify-between`
- 배경: `bg-gray-50`
- 테두리: `border-t border-gray-200`

**페이지 정보 (좌측)**
- 텍스트: "총 {total}개 중 {start}-{end} 표시"
- 폰트: `text-sm text-gray-700`
- 예시: "총 20개 중 1-10 표시"

**페이지 버튼 (우측)**
- 레이아웃: `flex items-center gap-1`
- 버튼 공통 스타일:
  ```typescript
  className="
    px-3 py-1.5 min-w-[40px]
    border border-gray-300 rounded
    text-sm font-medium
    transition-all duration-200
  "
  ```
- 이전 버튼:
  - 아이콘: `<ChevronLeft />`
  - Disabled: `opacity-50 cursor-not-allowed`
- 다음 버튼:
  - 아이콘: `<ChevronRight />`
  - Disabled: `opacity-50 cursor-not-allowed`
- 페이지 번호:
  - 기본: `bg-white text-gray-700 hover:bg-gray-50`
  - Active: `bg-[#236AF7] text-white border-[#236AF7]`
  - 생략 표시: `...` (ellipsis)

**페이지 표시 로직**
```typescript
// 최대 7개 페이지 버튼 표시
// 예: [이전] 1 2 3 4 5 ... 10 [다음]
const getPageNumbers = (currentPage: number, totalPages: number) => {
  const pages = [];
  const maxVisible = 7;

  if (totalPages <= maxVisible) {
    // 모든 페이지 표시
    for (let i = 1; i <= totalPages; i++) {
      pages.push(i);
    }
  } else {
    // 현재 페이지 중심으로 표시
    if (currentPage <= 4) {
      pages.push(1, 2, 3, 4, 5, '...', totalPages);
    } else if (currentPage >= totalPages - 3) {
      pages.push(1, '...', totalPages - 4, totalPages - 3, totalPages - 2, totalPages - 1, totalPages);
    } else {
      pages.push(1, '...', currentPage - 1, currentPage, currentPage + 1, '...', totalPages);
    }
  }

  return pages;
};
```

**페이지 크기**
- 기본: 10개/페이지
- 옵션: 10, 20, 50 (추후 구현 가능)

**반응형**
- Mobile: 이전/다음 버튼만 표시, 페이지 정보는 "1 / 10" 형식
- Tablet/Desktop: 전체 페이지네이션 표시

---

## 5. 페이지 3: 크리에이터 상세

### 5.1 전체 와이어프레임 (Desktop)

```
┌────────────────────────────────────────────────────────────────┐
│ [ZVZO Logo]  대시보드 | 소개                      [GitHub]      │ ← Header
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  [◄ 뒤로가기]                                                  │
│                                                                │
│  ┌──────────────────── 프로필 헤더 ──────────────────────┐    │
│  │ [Profile]  김지은           [IG] 250K  참여율 3.8%      │    │
│  │            가입일 2025-01-15                             │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │              [ AI 분석 시작 버튼 ]                      │    │
│  │   크리에이터의 판매 데이터를 AI가 분석합니다             │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 🤖 AI 분석 결과                                         │    │
│  │                                                         │    │
│  │ 강점:                                                   │    │
│  │ • Beauty 카테고리에서 88점 (상위 15%)                   │    │
│  │ • 30,000~50,000원 가격대 제품 강세                      │    │
│  │                                                         │    │
│  │ 추천:                                                   │    │
│  │ • 봄 시즌 뷰티 제품 집중 홍보                           │    │
│  │ • 중가 세럼/크림 제품군 확대                            │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 카테고리 강점                                           │    │
│  │ Beauty    ████████████████░░░░ 88                      │    │
│  │ Fashion   ████████████░░░░░░░░ 75                      │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 가격대 분포 (히스토그램)                                │    │
│  │   ┃                                                    │    │
│  │ 35┃     ███                                            │    │
│  │ 28┃ ███ ███                                            │    │
│  │ 14┃ ███ ███ ███                                        │    │
│  │   └─────────────                                       │    │
│  │   30-50K 50-80K 80-150K                                │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 시즌별 판매 추이 (라인 차트)                            │    │
│  │     ╱╲                                                 │    │
│  │    ╱  ╲        ╱╲                                      │    │
│  │ ──╱    ╲──────╱  ╲────                                 │    │
│  │  Spring Summer Fall Winter                             │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  [ 제품 매칭 분석 시작 버튼 ]                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

### 5.2 프로필 헤더

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  [◄ 뒤로가기]                                                  │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ [Profile]    김지은                                       │  │
│  │  Image                                                    │  │
│  │              [Instagram] 250,000 팔로워                   │  │
│  │              참여율 3.8% · 가입일 2025-01-15              │  │
│  │                                                           │  │
│  │              [Beauty] [Fashion]                           │  │
│  └────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**뒤로가기 버튼**
- 위치: 좌측 상단
- 스타일:
  ```typescript
  className="
    inline-flex items-center gap-2
    text-gray-600 hover:text-[#236AF7]
    font-medium transition-colors mb-6
  "
  ```
- 아이콘: `<ChevronLeft className="w-5 h-5" />`
- 텍스트: "뒤로가기"
- 링크: `/dashboard`

**프로필 카드**
- 배경: `bg-white`
- 패딩: `p-8`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-md`
- 여백: `mb-8`

**레이아웃**
- 레이아웃: `flex flex-col md:flex-row items-start md:items-center gap-6`

**프로필 이미지**
- 크기: `w-24 h-24 md:w-32 md:h-32`
- 모양: `rounded-full`
- 테두리: `ring-4 ring-gray-100`

**정보 영역**

1. **이름**
   - 폰트: `text-2xl md:text-3xl font-bold`
   - 색상: `text-gray-900`
   - 여백: `mb-2`

2. **플랫폼 뱃지 + 팔로워 수**
   - 레이아웃: `flex items-center gap-3 mb-3`
   - 플랫폼 뱃지:
     ```typescript
     className="
       inline-flex items-center gap-1.5 px-3 py-1.5
       bg-pink-100 text-pink-700
       text-sm font-medium rounded-full
     "
     ```
   - 팔로워 수:
     - 폰트: `text-lg font-semibold text-gray-900`
     - 포맷: "250,000 팔로워" 또는 "250K followers"

3. **부가 정보**
   - 레이아웃: `flex items-center gap-3 text-sm text-gray-600 mb-4`
   - 참여율: "참여율 3.8%"
   - 구분자: `·`
   - 가입일: "가입일 2025-01-15"

4. **카테고리 뱃지**
   - 레이아웃: `flex flex-wrap gap-2`
   - 각 뱃지:
     ```typescript
     className="
       px-3 py-1.5
       bg-[#E8F0FE] text-[#236AF7]
       text-sm font-medium rounded-full
     "
     ```

**참여율 계산**
```typescript
// 예시 (실제 데이터는 Mock Data에서 제공)
const engagementRate = 3.8; // %
```

---

### 5.3 AI 분석 전 상태

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                       [Sparkles Icon]                          │
│                                                                │
│                  AI 분석으로 인사이트 발견                      │
│                                                                │
│          크리에이터의 판매 데이터를 AI가 심층 분석하여          │
│       카테고리 강점, 최적 가격대, 시즌별 트렌드를 제공합니다    │
│                                                                │
│                  ┌──────────────────┐                          │
│                  │  AI 분석 시작     │                          │
│                  └──────────────────┘                          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: 그라데이션
  ```css
  bg-gradient-to-br from-[#E8F0FE] to-[#F0F4FF]
  ```
- 패딩: `p-12 md:p-16`
- 테두리: `rounded-xl border-2 border-[#236AF7]/20`
- 정렬: 중앙
- 여백: `mb-8`

**아이콘**
- 아이콘: `<Sparkles className="w-16 h-16 text-[#236AF7] mb-6" />`
- 애니메이션: 반짝임 (pulse + opacity)

**제목**
- 텍스트: "AI 분석으로 인사이트 발견"
- 폰트: `text-2xl md:text-3xl font-bold`
- 색상: `text-gray-900`
- 여백: `mb-4`

**설명**
- 폰트: `text-base md:text-lg`
- 색상: `text-gray-700`
- 정렬: 중앙
- 최대 너비: `max-w-2xl mx-auto`
- 여백: `mb-8`

**CTA 버튼**
- 텍스트: "AI 분석 시작"
- 스타일:
  ```typescript
  className="
    px-8 py-4
    bg-[#236AF7] hover:bg-[#1B5AD4]
    text-white font-semibold text-lg
    rounded-lg shadow-lg hover:shadow-xl
    transition-all duration-300
    transform hover:scale-105
  "
  ```
- 아이콘: `<Play className="inline w-5 h-5 mr-2" />`
- 클릭: API 호출 → AI 분석 실행

---

### 5.4 AI 분석 중 상태

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                        [Spinner]                               │
│                                                                │
│              크리에이터 데이터를 분석하고 있습니다...            │
│                                                                │
│                   예상 소요 시간: 약 10초                       │
│                                                                │
│                  ▓▓▓▓▓▓▓▓░░░░░░░░ 60%                          │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: `bg-white`
- 패딩: `p-12 md:p-16`
- 테두리: `rounded-xl border border-gray-200`
- 정렬: 중앙
- 여백: `mb-8`

**스피너**
- 아이콘: `<Loader2 className="w-12 h-12 text-[#236AF7] animate-spin mb-6" />`

**메시지**
- 텍스트: "크리에이터 데이터를 분석하고 있습니다..."
- 폰트: `text-lg font-medium`
- 색상: `text-gray-900`
- 여백: `mb-4`

**예상 시간**
- 텍스트: "예상 소요 시간: 약 10초"
- 폰트: `text-sm`
- 색상: `text-gray-600`
- 여백: `mb-6`

**진행 바**
- 너비: `w-full max-w-md`
- 높이: `h-2`
- 배경: `bg-gray-200 rounded-full`
- 진행:
  ```typescript
  className="
    h-full bg-gradient-to-r from-[#236AF7] to-[#1B5AD4]
    rounded-full transition-all duration-500
  "
  style={{ width: `${progress}%` }}
  ```
- 진행률 텍스트: `text-sm font-medium text-gray-700 mt-2`

**진행 상태 시뮬레이션**
```typescript
const [progress, setProgress] = useState(0);

useEffect(() => {
  const interval = setInterval(() => {
    setProgress(prev => {
      if (prev >= 100) {
        clearInterval(interval);
        return 100;
      }
      return prev + 10;
    });
  }, 1000); // 10초 동안 0% → 100%

  return () => clearInterval(interval);
}, []);
```

---

### 5.5 AI 분석 완료 상태

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  🤖 AI 분석 결과                                   [완료 2026-02-04] │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 💡 핵심 인사이트                                        │
│  │                                                         │
│  │ Beauty 카테고리에서 탁월한 성과를 보이고 있으며         │
│  │ 30,000~50,000원 가격대 제품에서 특히 강세를 보입니다    │
│  │ 봄 시즌 매출이 30% 증가하는 패턴이 관찰되었습니다       │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  강점 (Strengths)                                              │
│  ────────────                                                  │
│  ✓ Beauty 카테고리 88점 (상위 15%)                            │
│  ✓ 30,000~50,000원 가격대 제품 강세                           │
│  ✓ 평균 전환율 4.2% (업계 평균 3.1% 대비 +35%)                │
│  ✓ 봄 시즌 뷰티 제품 판매 30% 증가                            │
│                                                                │
│  추천 사항 (Recommendations)                                   │
│  ──────────                                                    │
│  → 봄 시즌 뷰티 제품 집중 홍보 전략                           │
│  → 30,000~50,000원 중가 세럼/크림 제품군 확대                 │
│  → Fashion 카테고리 추가 공략 (현재 75점)                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: `bg-white`
- 패딩: `p-8`
- 테두리: `rounded-xl border-2 border-[#236AF7]`
- 그림자: `shadow-lg`
- 여백: `mb-8`

**헤더**
- 레이아웃: `flex items-center justify-between mb-6`
- 제목:
  - 아이콘: `🤖` + 텍스트: "AI 분석 결과"
  - 폰트: `text-2xl font-bold`
  - 색상: `text-gray-900`
- 완료 뱃지:
  - 텍스트: "완료 2026-02-04"
  - 스타일:
    ```typescript
    className="
      px-3 py-1.5 bg-green-100 text-green-700
      text-sm font-medium rounded-full
    "
    ```

**핵심 인사이트 카드**
- 배경: 그라데이션
  ```css
  bg-gradient-to-r from-[#E8F0FE] to-[#F0F4FF]
  ```
- 패딩: `p-6`
- 테두리: `rounded-lg border border-[#236AF7]/20`
- 여백: `mb-6`
- 아이콘: `💡` (좌측 상단)
- 제목: "핵심 인사이트" (`text-lg font-semibold mb-3`)
- 내용: AI가 생성한 2-3문장 요약 (`text-gray-700 leading-relaxed`)

**강점 섹션**
- 제목: "강점 (Strengths)" (`text-lg font-semibold text-gray-900 mb-3`)
- 하단 경계선: `border-b-2 border-gray-200 pb-2 mb-4`
- 목록:
  - 레이아웃: `space-y-2`
  - 각 항목:
    - 아이콘: `✓` (녹색 체크마크) `<Check className="w-5 h-5 text-green-600" />`
    - 폰트: `text-base text-gray-700`
    - 강조: 숫자/비율은 `font-semibold text-gray-900`

**추천 사항 섹션**
- 제목: "추천 사항 (Recommendations)" (`text-lg font-semibold text-gray-900 mb-3 mt-6`)
- 하단 경계선: `border-b-2 border-gray-200 pb-2 mb-4`
- 목록:
  - 레이아웃: `space-y-2`
  - 각 항목:
    - 아이콘: `→` (화살표) `<ArrowRight className="w-5 h-5 text-[#236AF7]" />`
    - 폰트: `text-base text-gray-700`

---

### 5.6 카테고리 강점 차트 (수평 막대 차트)

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  카테고리별 성과 점수                                           │
│                                                                │
│  Beauty      ████████████████░░░░ 88                           │
│  Fashion     ████████████░░░░░░░░ 75                           │
│  Lifestyle   ████████░░░░░░░░░░░░ 62                           │
│                                                                │
│              0    20   40   60   80   100                      │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-sm`
- 여백: `mb-8`

**제목**
- 텍스트: "카테고리별 성과 점수"
- 폰트: `text-xl font-semibold`
- 색상: `text-gray-900`
- 여백: `mb-6`

**차트 구현 (Recharts BarChart)**

```typescript
import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Cell } from 'recharts';

// 데이터 준비
const categoryScores = [
  { category: 'Beauty', score: 88, color: '#236AF7' },
  { category: 'Fashion', score: 75, color: '#9333EA' },
  { category: 'Lifestyle', score: 62, color: '#10B981' },
];

// 차트 설정
const chartConfig = {
  width: '100%',
  height: 300,
  layout: 'horizontal',
  margin: { top: 20, right: 30, left: 100, bottom: 20 },
};

// Recharts 컴포넌트
<ResponsiveContainer width="100%" height={300}>
  <BarChart
    data={categoryScores}
    layout="vertical"
    margin={{ top: 20, right: 30, left: 20, bottom: 20 }}
  >
    <CartesianGrid strokeDasharray="3 3" stroke="#E5E7EB" />
    <XAxis
      type="number"
      domain={[0, 100]}
      tickCount={6}
      stroke="#6B7280"
      style={{ fontSize: '12px' }}
    />
    <YAxis
      type="category"
      dataKey="category"
      stroke="#6B7280"
      style={{ fontSize: '14px', fontWeight: 500 }}
    />
    <Tooltip
      contentStyle={{
        backgroundColor: '#FFFFFF',
        border: '1px solid #E5E7EB',
        borderRadius: '8px',
        fontSize: '14px',
      }}
      formatter={(value) => [`${value}점`, '성과 점수']}
    />
    <Bar dataKey="score" radius={[0, 8, 8, 0]}>
      {categoryScores.map((entry, index) => (
        <Cell key={`cell-${index}`} fill={entry.color} />
      ))}
    </Bar>
  </BarChart>
</ResponsiveContainer>
```

**점수 색상 기준**
```typescript
const getScoreColor = (score: number) => {
  if (score >= 80) return '#236AF7'; // 블루 (우수)
  if (score >= 60) return '#10B981'; // 그린 (양호)
  if (score >= 40) return '#F59E0B'; // 오렌지 (보통)
  return '#EF4444'; // 레드 (개선 필요)
};
```

---

### 5.7 가격대 분포 히스토그램

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  가격대별 판매 분포                                             │
│                                                                │
│   판매 건수                                                     │
│   40 ┃                                                         │
│   35 ┃     ███                                                 │
│   30 ┃     ███                                                 │
│   25 ┃ ███ ███ ███                                             │
│   20 ┃ ███ ███ ███                                             │
│      └─────────────────                                        │
│      10-30K 30-50K 50-80K 80-150K                              │
│              가격 범위 (KRW)                                    │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-sm`
- 여백: `mb-8`

**제목**
- 텍스트: "가격대별 판매 분포"
- 폰트: `text-xl font-semibold`
- 색상: `text-gray-900`
- 여백: `mb-6`

**차트 구현 (Recharts BarChart)**

```typescript
// 데이터 준비
const priceBuckets = [
  { range: '10-30K', count: 20, revenue: 450000 },
  { range: '30-50K', count: 35, revenue: 1400000 },
  { range: '50-80K', count: 28, revenue: 1820000 },
  { range: '80-150K', count: 14, revenue: 1540000 },
];

// Recharts 컴포넌트
<ResponsiveContainer width="100%" height={300}>
  <BarChart
    data={priceBuckets}
    margin={{ top: 20, right: 30, left: 20, bottom: 60 }}
  >
    <CartesianGrid strokeDasharray="3 3" stroke="#E5E7EB" />
    <XAxis
      dataKey="range"
      stroke="#6B7280"
      angle={-45}
      textAnchor="end"
      height={80}
      style={{ fontSize: '12px' }}
    />
    <YAxis
      label={{
        value: '판매 건수',
        angle: -90,
        position: 'insideLeft',
        style: { fontSize: '14px', fill: '#6B7280' },
      }}
      stroke="#6B7280"
      style={{ fontSize: '12px' }}
    />
    <Tooltip
      contentStyle={{
        backgroundColor: '#FFFFFF',
        border: '1px solid #E5E7EB',
        borderRadius: '8px',
        fontSize: '14px',
      }}
      formatter={(value, name) => {
        if (name === 'count') return [`${value}건`, '판매 건수'];
        return [`${(value / 10000).toFixed(0)}만원`, '총 매출'];
      }}
    />
    <Bar
      dataKey="count"
      fill="#236AF7"
      radius={[8, 8, 0, 0]}
    />
  </BarChart>
</ResponsiveContainer>
```

**툴팁 내용**
- 가격 범위: "30-50K"
- 판매 건수: "35건"
- 총 매출: "140만원"

---

### 5.8 시즌별 판매 추이 라인 차트

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  시즌별 판매 추이                                               │
│                                                                │
│   매출 (만원)                                                   │
│   200 ┃        ╱╲                                              │
│   180 ┃       ╱  ╲                                             │
│   160 ┃      ╱    ╲      ╱╲                                    │
│   140 ┃     ╱      ╲    ╱  ╲                                   │
│   120 ┃    ╱        ╲  ╱    ╲                                  │
│   100 ┃ ──╱          ╲╱      ╲────                             │
│       └──────────────────────────                              │
│       Spring  Summer  Fall  Winter                             │
│                 시즌                                            │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-sm`
- 여백: `mb-8`

**제목**
- 텍스트: "시즌별 판매 추이"
- 폰트: `text-xl font-semibold`
- 색상: `text-gray-900`
- 여백: `mb-6`

**차트 구현 (Recharts LineChart)**

```typescript
// 데이터 준비
const seasonalData = [
  { season: 'Spring', revenue: 1850000, salesCount: 25 },
  { season: 'Summer', revenue: 1350000, salesCount: 18 },
  { season: 'Fall', revenue: 1600000, salesCount: 20 },
  { season: 'Winter', revenue: 850000, salesCount: 14 },
];

// Recharts 컴포넌트
<ResponsiveContainer width="100%" height={300}>
  <LineChart
    data={seasonalData}
    margin={{ top: 20, right: 30, left: 20, bottom: 20 }}
  >
    <CartesianGrid strokeDasharray="3 3" stroke="#E5E7EB" />
    <XAxis
      dataKey="season"
      stroke="#6B7280"
      style={{ fontSize: '12px' }}
    />
    <YAxis
      label={{
        value: '매출 (만원)',
        angle: -90,
        position: 'insideLeft',
        style: { fontSize: '14px', fill: '#6B7280' },
      }}
      tickFormatter={(value) => (value / 10000).toFixed(0)}
      stroke="#6B7280"
      style={{ fontSize: '12px' }}
    />
    <Tooltip
      contentStyle={{
        backgroundColor: '#FFFFFF',
        border: '1px solid #E5E7EB',
        borderRadius: '8px',
        fontSize: '14px',
      }}
      formatter={(value, name) => {
        if (name === 'revenue') return [`${(value / 10000).toFixed(0)}만원`, '매출'];
        return [`${value}건`, '판매 건수'];
      }}
    />
    <Line
      type="monotone"
      dataKey="revenue"
      stroke="#236AF7"
      strokeWidth={3}
      dot={{ r: 6, fill: '#236AF7', strokeWidth: 2, stroke: '#FFFFFF' }}
      activeDot={{ r: 8 }}
    />
  </LineChart>
</ResponsiveContainer>
```

**계절별 한글 표시**
```typescript
const seasonLabels = {
  Spring: '봄',
  Summer: '여름',
  Fall: '가을',
  Winter: '겨울',
};
```

---

### 5.9 전환 지표 카드

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  주요 전환 지표                                                 │
│                                                                │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐                   │
│  │ 평균     │   │ 최고     │   │ 팔로워   │                   │
│  │ 전환율   │   │ 카테고리 │   │ 대비     │                   │
│  │          │   │          │   │          │                   │
│  │ 4.2%     │   │ Beauty   │   │ 1.68%    │                   │
│  │ ▲ +0.8%p │   │ 88점     │   │ 상위 20% │                   │
│  └──────────┘   └──────────┘   └──────────┘                   │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 레이아웃: `grid grid-cols-1 md:grid-cols-3 gap-6`
- 여백: `mb-8`

**카드 공통 스타일**
- 배경: `bg-gradient-to-br from-white to-gray-50`
- 패딩: `p-6`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-sm hover:shadow-md transition-shadow`

**카드 구조**

1. **레이블**
   - 폰트: `text-sm font-medium`
   - 색상: `text-gray-600`
   - 여백: `mb-3`

2. **주요 값**
   - 폰트: `text-3xl font-bold`
   - 색상: `text-gray-900`
   - 여백: `mb-2`

3. **부가 정보**
   - 폰트: `text-sm`
   - 색상: `text-gray-600`
   - 아이콘: 변화량은 `<TrendingUp />` 또는 `<TrendingDown />`

**카드 데이터**

| 카드 | 레이블 | 값 | 부가 정보 |
|------|--------|-----|----------|
| 1 | 평균 전환율 | 4.2% | ▲ +0.8%p (전월 대비) |
| 2 | 최고 카테고리 | Beauty | 88점 (상위 15%) |
| 3 | 팔로워 대비 전환 | 1.68% | 상위 20% |

---

### 5.10 판매 이력 테이블 (확장 가능)

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  최근 판매 이력 (20건)                                          │
│                                                                │
│  날짜        제품명          카테고리   수량   금액   전환율    │
│  ──────────────────────────────────────────────────────────   │
│  2025-12-18  글로우 세럼    Beauty     5     225K    4.2%     │
│  2025-11-20  데일리 청바지  Fashion    3     267K    3.5%     │
│  ...                                                            │
│                                                                │
│  [더 보기]                                                      │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-sm`
- 여백: `mb-8`

**제목**
- 텍스트: "최근 판매 이력 (20건)"
- 폰트: `text-xl font-semibold`
- 색상: `text-gray-900`
- 여백: `mb-6`

**테이블**
- 기본 표시: 5건
- 확장 시: 20건 전체

**더 보기 버튼**
- 텍스트: "더 보기" / "접기"
- 스타일:
  ```typescript
  className="
    w-full py-3 mt-4
    bg-gray-100 hover:bg-gray-200
    text-gray-700 font-medium
    rounded-lg transition-colors
  "
  ```

---

### 5.11 제품 매칭 CTA

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                  🎯 다음 단계: 제품 매칭 분석                   │
│                                                                │
│         AI가 이 크리에이터에게 최적의 제품을 추천합니다         │
│                                                                │
│                  ┌────────────────────┐                        │
│                  │ 제품 매칭 분석 시작 │                        │
│                  └────────────────────┘                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: 그라데이션
  ```css
  bg-gradient-to-r from-[#236AF7] to-[#1B5AD4]
  ```
- 패딩: `p-12`
- 테두리: `rounded-xl`
- 정렬: 중앙

**아이콘 + 제목**
- 아이콘: `🎯`
- 제목: "다음 단계: 제품 매칭 분석"
- 폰트: `text-2xl font-bold`
- 색상: `text-white`
- 여백: `mb-4`

**설명**
- 텍스트: "AI가 이 크리에이터에게 최적의 제품을 추천합니다"
- 폰트: `text-lg`
- 색상: `text-white/90`
- 여백: `mb-8`

**CTA 버튼**
- 텍스트: "제품 매칭 분석 시작"
- 스타일:
  ```typescript
  className="
    px-8 py-4
    bg-white hover:bg-gray-100
    text-[#236AF7] font-semibold text-lg
    rounded-lg shadow-xl
    transition-all duration-300
    transform hover:scale-105
  "
  ```
- 링크: `/creator/${creatorId}/match`

---

### 5.12 에러 상태

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│                                                                │
│                      [AlertCircle Icon]                        │
│                                                                │
│                    분석 중 오류가 발생했습니다                  │
│                                                                │
│      데이터를 불러오는 중 문제가 발생했습니다.                  │
│               잠시 후 다시 시도해주세요.                        │
│                                                                │
│                  ┌──────────────┐                              │
│                  │  다시 시도    │                              │
│                  └──────────────┘                              │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 배경: `bg-red-50`
- 패딩: `p-12`
- 테두리: `rounded-xl border-2 border-red-200`
- 정렬: 중앙

**아이콘**
- 아이콘: `<AlertCircle className="w-16 h-16 text-red-600 mb-6" />`

**제목**
- 텍스트: "분석 중 오류가 발생했습니다"
- 폰트: `text-xl font-bold`
- 색상: `text-red-900`
- 여백: `mb-4`

**설명**
- 폰트: `text-base`
- 색상: `text-red-700`
- 여백: `mb-8`

**재시도 버튼**
- 텍스트: "다시 시도"
- 스타일:
  ```typescript
  className="
    px-6 py-3
    bg-red-600 hover:bg-red-700
    text-white font-semibold
    rounded-lg transition-colors
  "
  ```

---

## 6. 페이지 4: AI 제품 매칭

### 6.1 전체 와이어프레임 (Desktop)

```
┌────────────────────────────────────────────────────────────────┐
│ [ZVZO Logo]  대시보드 | 소개                      [GitHub]      │
├────────────────────────────────────────────────────────────────┤
│ [◄ 뒤로가기]                                                   │
│                                                                │
│ ┌───────── 좌측 사이드 ──────┐  ┌───── 우측 메인 ─────────┐  │
│ │ 김지은님을 위한 추천 제품    │  │ 추천 제품 #1 (매칭 92%)  │  │
│ │                             │  │                          │  │
│ │ [Profile Image]             │  │ ┌──────────────────────┐ │  │
│ │ 김지은                       │  │ │ 매칭 스코어 원형     │ │  │
│ │ Instagram · 250K            │  │ │      게이지          │ │  │
│ │                             │  │ │        92            │ │  │
│ │ 강점 카테고리:               │  │ └──────────────────────┘ │  │
│ │ • Beauty (88점)             │  │                          │  │
│ │ • Fashion (75점)            │  │ 스코어 breakdown:        │  │
│ │                             │  │ Category  ████████ 95    │  │
│ │ 최적 가격대:                 │  │ Price     ████████ 90    │  │
│ │ 30,000~50,000원             │  │ Season    ███████░ 88    │  │
│ │                             │  │ Audience  ████████ 92    │  │
│ │                             │  │                          │  │
│ └─────────────────────────────┘  │ [제품 이미지]            │  │
│                                  │ 글로우 세럼              │  │
│                                  │ 글로우랩                 │  │
│                                  │ Beauty · 45,000원       │  │
│                                  │                          │  │
│                                  │ 💡 AI 추천 이유:         │  │
│                                  │ "Beauty 카테고리에서..." │  │
│                                  │                          │  │
│                                  │ 매출 예측 범위:          │  │
│                                  │ ▓▓▓▓▓▓▓▓▓░ 125만원      │  │
│                                  │ Min 95만 | Max 155만    │  │
│                                  │                          │  │
│                                  │ [신뢰도: 높음]           │  │
│                                  └──────────────────────────┘  │
│                                  ...4개 더                     │
│                                                                │
│  [선택한 제품 비교 (하단 패널)]                                │
└────────────────────────────────────────────────────────────────┘
```

---

### 6.2 좌측 사이드 패널 (크리에이터 요약)

#### ASCII 와이어프레임

```
┌─────────────────────────────┐
│ 김지은님을 위한 추천 제품    │
│                             │
│ ┌───────────────────────┐  │
│ │   [Profile Image]      │  │
│ └───────────────────────┘  │
│                             │
│ 김지은                       │
│ Instagram · 250,000 팔로워  │
│                             │
│ ─────────────────────────  │
│                             │
│ 강점 카테고리:               │
│ • Beauty (88점)             │
│ • Fashion (75점)            │
│                             │
│ 최적 가격대:                 │
│ 30,000~50,000원             │
│                             │
│ 평균 전환율:                 │
│ 4.2%                        │
│                             │
│ 총 매출:                     │
│ 565만원                     │
└─────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 너비: `w-80` (Desktop), `w-full` (Mobile - 상단 배치)
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border border-gray-200`
- 그림자: `shadow-md`
- 위치: `sticky top-6` (Desktop에서 스크롤 시 고정)

**제목**
- 텍스트: "{이름}님을 위한 추천 제품"
- 폰트: `text-lg font-semibold`
- 색상: `text-gray-900`
- 여백: `mb-6`

**프로필 이미지**
- 크기: `w-20 h-20`
- 모양: `rounded-full mx-auto`
- 테두리: `ring-4 ring-gray-100`
- 여백: `mb-4`

**이름 + 플랫폼**
- 이름: `text-xl font-bold text-gray-900`
- 플랫폼: `text-sm text-gray-600` (아이콘 + 팔로워 수)
- 여백: `mb-6`

**구분선**
- 스타일: `border-t border-gray-200`
- 여백: `my-6`

**정보 섹션**
- 각 섹션:
  - 레이블: `text-sm font-medium text-gray-600 mb-2`
  - 값: `text-base font-semibold text-gray-900`
  - 여백: `mb-4`

**강점 카테고리 목록**
- 레이아웃: `space-y-1`
- 각 항목:
  - 아이콘: `•`
  - 텍스트: "{카테고리} ({점수}점)"
  - 색상: 점수에 따라 변화

---

### 6.3 추천 제품 카드

#### ASCII 와이어프레임

```
┌──────────────────────────────────────────────────────────────┐
│  추천 제품 #1                                     매칭 92%    │
│                                                              │
│  ┌────────────┐                                             │
│  │   매칭      │                                             │
│  │  스코어     │    스코어 세부 분석:                         │
│  │    92       │    Category   ████████░░ 95                │
│  │   원형      │    Price      ███████░░░ 90                │
│  │  게이지     │    Season     ███████░░░ 88                │
│  └────────────┘    Audience   ████████░░ 92                │
│                                                              │
│  ┌────────────────────┐                                     │
│  │   [제품 이미지]     │                                     │
│  └────────────────────┘                                     │
│                                                              │
│  글로우 세럼                                                 │
│  글로우랩                                                    │
│  [Beauty] · 45,000원                                        │
│                                                              │
│  💡 AI 추천 이유:                                            │
│  "김지은님은 Beauty 카테고리에서 평균 4.2%의 높은 전환율을    │
│   기록 중이며, 30,000~50,000원 가격대 제품에서 강세를 보입   │
│   니다. 이 세럼은 해당 가격대에 속하며..."                   │
│                                                              │
│  📊 매출 예측 범위:                                          │
│  ▓▓▓▓▓▓▓▓▓░░░░░░░ 125만원                                   │
│  Min 95만원 | Expected 125만원 | Max 155만원                │
│                                                              │
│  [신뢰도: 높음]                    [☐ 비교 추가]             │
└──────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**카드 컨테이너**
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `rounded-xl border-2 border-[#236AF7]/20`
- 그림자: `shadow-lg hover:shadow-xl`
- 전환: `transition-all duration-300`
- 여백: `mb-6`
- 호버: `hover:border-[#236AF7]/40 hover:scale-[1.02]`

**헤더**
- 레이아웃: `flex items-center justify-between mb-6`
- 제목: "추천 제품 #{순위}" (`text-lg font-semibold text-gray-900`)
- 매칭 스코어 뱃지:
  ```typescript
  className="
    px-4 py-2
    bg-[#236AF7] text-white
    text-lg font-bold rounded-full
  "
  ```
  - 텍스트: "매칭 {score}%"

**매칭 스코어 원형 게이지**
- 크기: `w-32 h-32`
- 위치: 좌측 상단
- 구현: SVG 또는 Recharts RadialBarChart
- 색상: 점수에 따라 변화
  - 90-100: `#236AF7` (블루)
  - 70-89: `#10B981` (그린)
  - 50-69: `#F59E0B` (오렌지)
  - 0-49: `#EF4444` (레드)

```typescript
// Recharts RadialBarChart
<RadialBarChart
  width={128}
  height={128}
  cx={64}
  cy={64}
  innerRadius={50}
  outerRadius={64}
  barSize={14}
  data={[{ score: 92, fill: '#236AF7' }]}
  startAngle={90}
  endAngle={-270}
>
  <PolarAngleAxis type="number" domain={[0, 100]} tick={false} />
  <RadialBar dataKey="score" cornerRadius={10} />
  <text
    x={64}
    y={64}
    textAnchor="middle"
    dominantBaseline="middle"
    className="text-2xl font-bold"
    fill="#111827"
  >
    {92}
  </text>
</RadialBarChart>
```

**스코어 세부 분석 (4개 바)**
- 레이아웃: 우측에 세로 스택 (`space-y-3`)
- 각 바:
  - 레이블: `text-sm font-medium text-gray-700`
  - 진행 바:
    - 배경: `bg-gray-200 h-2 rounded-full`
    - 진행: `bg-[#236AF7] h-2 rounded-full` (너비는 점수%)
  - 점수: `text-sm font-semibold text-gray-900`

**항목:**
1. Category (카테고리 일치도) - 0~100
2. Price (가격대 적합도) - 0~100
3. Season (시즌 트렌드) - 0~100
4. Audience (팔로워 특성) - 0~100

**제품 이미지**
- 크기: `w-full h-48` (aspect ratio 16:9)
- 모양: `rounded-lg`
- 배경: `bg-gray-100` (플레이스홀더)
- 여백: `mb-4`

**제품 정보**
- 제품명: `text-xl font-bold text-gray-900 mb-1`
- 브랜드명: `text-sm text-gray-600 mb-2`
- 카테고리 + 가격:
  - 레이아웃: `flex items-center gap-2`
  - 카테고리 뱃지: `px-2.5 py-1 bg-pink-100 text-pink-700 text-xs font-medium rounded-full`
  - 가격: `text-lg font-semibold text-gray-900`
- 여백: `mb-6`

**AI 추천 이유**
- 아이콘: `💡` 또는 `<Lightbulb className="w-5 h-5 text-yellow-500" />`
- 제목: "AI 추천 이유:" (`text-sm font-medium text-gray-700 mb-2`)
- 내용:
  - 폰트: `text-sm italic`
  - 색상: `text-gray-600`
  - 줄 간격: `leading-relaxed`
  - 배경: `bg-[#F0F4FF] p-4 rounded-lg`
  - 여백: `mb-6`

**매출 예측 범위**
- 아이콘: `📊`
- 제목: "매출 예측 범위:" (`text-sm font-medium text-gray-700 mb-3`)
- 예상 매출 (중앙 값):
  - 폰트: `text-2xl font-bold`
  - 색상: `text-[#236AF7]`
  - 포맷: "125만원"
  - 여백: `mb-2`
- 예측 범위 바:
  - 높이: `h-3`
  - 배경: `bg-gray-200 rounded-full`
  - 그라데이션:
    ```css
    bg-gradient-to-r from-yellow-400 via-green-500 to-blue-500
    ```
  - 3개 마커: Min (좌측), Expected (중앙), Max (우측)
- 범위 텍스트:
  - 레이아웃: `flex justify-between`
  - 폰트: `text-xs text-gray-600`
  - 포맷: "Min 95만원 | Expected 125만원 | Max 155만원"
- 여백: `mb-6`

**신뢰도 배지 + 비교 체크박스**
- 레이아웃: `flex items-center justify-between`
- 신뢰도 배지:
  - 높음: `bg-green-100 text-green-700`
  - 중간: `bg-yellow-100 text-yellow-700`
  - 낮음: `bg-red-100 text-red-700`
  - 스타일: `px-3 py-1.5 text-sm font-medium rounded-full`
- 비교 체크박스:
  - 레이블: "비교 추가"
  - 스타일: shadcn/ui Checkbox 컴포넌트

---

### 6.4 제품 비교 패널 (하단 슬라이드업)

#### ASCII 와이어프레임

```
┌────────────────────────────────────────────────────────────────┐
│  선택한 제품 비교 (3개)                              [✕ 닫기]  │
│                                                                │
│  ┌────────────┐   ┌────────────┐   ┌────────────┐            │
│  │ 글로우 세럼 │   │ 데일리     │   │ 프로틴바   │            │
│  │ 45,000원   │   │ 청바지     │   │ 28,000원   │            │
│  │            │   │ 89,000원   │   │            │            │
│  │ 매칭: 92%  │   │ 매칭: 85%  │   │ 매칭: 78%  │            │
│  │ 예측: 125만│   │ 예측: 98만 │   │ 예측: 65만 │            │
│  │ 신뢰: 높음 │   │ 신뢰: 높음 │   │ 신뢰: 중간 │            │
│  └────────────┘   └────────────┘   └────────────┘            │
│                                                                │
│  [비교 초기화]                                                 │
└────────────────────────────────────────────────────────────────┘
```

#### 상세 스펙

**컨테이너**
- 위치: 화면 하단 고정 (`fixed bottom-0 left-0 right-0`)
- 배경: `bg-white`
- 패딩: `p-6`
- 테두리: `border-t-2 border-gray-200`
- 그림자: `shadow-2xl`
- 애니메이션: 하단에서 슬라이드업
  ```css
  transform: translateY(100%); /* 숨김 */
  transform: translateY(0);    /* 표시 */
  transition: transform 0.3s ease-out;
  ```
- z-index: `z-50`

**헤더**
- 레이아웃: `flex items-center justify-between mb-6`
- 제목: "선택한 제품 비교 ({개수}개)" (`text-xl font-semibold`)
- 닫기 버튼: `<X className="w-6 h-6" />`

**제품 카드 그리드**
- 레이아웃: `grid grid-cols-1 md:grid-cols-3 gap-4`
- 최대: 3개 제품 (초과 시 경고)

**비교 카드**
- 배경: `bg-gray-50`
- 패딩: `p-4`
- 테두리: `rounded-lg border border-gray-200`
- 구조:
  - 제품 이미지 (작게) - `w-full h-24`
  - 제품명 - `text-sm font-semibold`
  - 가격 - `text-base font-bold text-gray-900`
  - 매칭 스코어 - `text-sm`
  - 예측 매출 - `text-sm`
  - 신뢰도 - `text-xs`

**비교 초기화 버튼**
- 텍스트: "비교 초기화"
- 스타일:
  ```typescript
  className="
    w-full py-3 mt-4
    bg-gray-100 hover:bg-gray-200
    text-gray-700 font-medium
    rounded-lg transition-colors
  "
  ```

---

### 6.5 로딩 및 에러 상태

**로딩 상태**
- 스피너 + "AI가 최적의 제품을 분석하고 있습니다..."
- 스켈레톤: 5개 카드 (animate-pulse)

**에러 상태**
- 아이콘: `<AlertCircle />`
- 메시지: "제품 매칭 분석 중 오류가 발생했습니다"
- 재시도 버튼

---

## 7. 컴포넌트 라이브러리

### 7.1 공통 컴포넌트 목록

| 컴포넌트 | 경로 | 용도 |
|----------|------|------|
| Button | `@/components/ui/button` | 버튼 (shadcn/ui) |
| Card | `@/components/ui/card` | 카드 (shadcn/ui) |
| Input | `@/components/ui/input` | 입력 필드 (shadcn/ui) |
| Select | `@/components/ui/select` | 드롭다운 (shadcn/ui) |
| Table | `@/components/ui/table` | 테이블 (shadcn/ui) |
| Badge | `@/components/ui/badge` | 뱃지 (shadcn/ui) |
| Skeleton | `@/components/ui/skeleton` | 스켈레톤 (shadcn/ui) |
| Dialog | `@/components/ui/dialog` | 모달 (shadcn/ui) |
| Tabs | `@/components/ui/tabs` | 탭 (shadcn/ui) |

### 7.2 커스텀 컴포넌트

| 컴포넌트 | 경로 | 설명 |
|----------|------|------|
| Header | `@/components/layout/header` | 공통 헤더 |
| Footer | `@/components/layout/footer` | 공통 푸터 |
| PageContainer | `@/components/layout/page-container` | 페이지 래퍼 |
| CreatorCard | `@/components/dashboard/creator-card` | 크리에이터 카드 |
| InsightCard | `@/components/creator/insight-card` | AI 인사이트 카드 |
| CategoryChart | `@/components/charts/category-chart` | 카테고리 차트 |
| PriceHistogram | `@/components/charts/price-histogram` | 가격 히스토그램 |
| SeasonalChart | `@/components/charts/seasonal-chart` | 시즌 차트 |
| ProductMatchCard | `@/components/match/product-match-card` | 제품 매칭 카드 |
| ScoreGauge | `@/components/match/score-gauge` | 매칭 스코어 게이지 |

---

## 8. 애니메이션 및 인터랙션

### 8.1 페이지 전환 애니메이션

```typescript
// Framer Motion variants
const pageVariants = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -20 },
};

const pageTransition = {
  duration: 0.3,
  ease: 'easeOut',
};
```

### 8.2 스크롤 애니메이션 (랜딩 페이지)

```typescript
// Intersection Observer API 사용
const fadeInOnScroll = {
  initial: { opacity: 0, y: 50 },
  whileInView: { opacity: 1, y: 0 },
  viewport: { once: true, margin: '-100px' },
  transition: { duration: 0.6, ease: 'easeOut' },
};
```

### 8.3 호버 인터랙션

```typescript
// 카드 호버
const cardHover = {
  scale: 1.02,
  boxShadow: '0 20px 25px -5px rgba(0, 0, 0, 0.1)',
  transition: { duration: 0.2 },
};

// 버튼 호버
const buttonHover = {
  scale: 1.05,
  transition: { duration: 0.2 },
};
```

### 8.4 로딩 애니메이션

```typescript
// 스피너
const spinnerVariants = {
  animate: {
    rotate: 360,
    transition: {
      duration: 1,
      repeat: Infinity,
      ease: 'linear',
    },
  },
};

// 진행 바
const progressVariants = {
  initial: { width: '0%' },
  animate: (progress: number) => ({
    width: `${progress}%`,
    transition: { duration: 0.5, ease: 'easeOut' },
  }),
};
```

---

## 부록: 반응형 체크리스트

### Mobile (~768px)
- [ ] 헤더: 햄버거 메뉴 작동
- [ ] 히어로: 제목 2줄, 버튼 full-width
- [ ] 카드: 1열 세로 스택
- [ ] 테이블: 가로 스크롤 가능
- [ ] 사이드 패널: 상단 배치
- [ ] 페이지네이션: 간소화 (이전/다음만)

### Tablet (768px~1024px)
- [ ] 헤더: 전체 네비게이션 표시
- [ ] 카드: 2열 그리드
- [ ] 필터: 2x2 그리드
- [ ] 차트: 적절한 크기 유지

### Desktop (1024px~)
- [ ] 모든 요소 최대 크기
- [ ] 사이드 패널: sticky 동작
- [ ] 호버 효과 작동
- [ ] 차트: 최적 비율

---

**문서 버전**: 1.0.0
**최종 업데이트**: 2026-02-04
**작성자**: lang-typescript-expert
**상태**: 초안 완료

